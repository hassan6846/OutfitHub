import { createSlice } from "@reduxjs/toolkit";

const LikedSlice=createSlice({
    name:"like",
    initialState:{
        product:[],
        
    },
    reducers:{
        ///sync the liked
        // clear like
        //add to like 
        //remove item from likes
    
    }
})
