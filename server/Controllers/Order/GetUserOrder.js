const Order = require("../../models/OrderModel");

const GetUserOrderNo = async (req, res, next) => {
    const id = req.query
    //find order no by id
    try {
    
    } catch (error) {
        console.log(error.message)
    }

}
module.exports = GetUserOrderNo